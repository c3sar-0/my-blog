- User tab on header *
- Render header content conditionally *
- New Post page *
- Edit Post page *
- Finish post previews (icons, title)
- User page
- Tags
- Make image optional, if not image, the preview is just the rest (it's smaller)
- When editing or creating a post, if I leave without saving the images uploaded to the server should be deleted.
- /api lists endpoints
- Send number of comments and if user has bookmarked in post view
  
  CHECK TIPTAP TEXT EDITOR